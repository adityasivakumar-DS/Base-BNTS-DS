{
  "$schema": "https://tr.designtokens.org/format/",
  "motion": {
    "duration": {
      "instant":   { "$value": "0ms",    "$type": "duration", "$description": "Immediate — no perceived animation" },
      "fast":      { "$value": "150ms",  "$type": "duration", "$description": "Fast — micro-interactions" },
      "normal":    { "$value": "250ms",  "$type": "duration", "$description": "Standard — component transitions" },
      "slow":      { "$value": "400ms",  "$type": "duration", "$description": "Slow — page-level transitions" },
      "slower":    { "$value": "600ms",  "$type": "duration", "$description": "Very slow — elaborate animations" },
      "very-slow": { "$value": "1000ms", "$type": "duration", "$description": "Long — orchestrated sequences" }
    },
    "easing": {
      "linear":      { "$value": "linear",                      "$type": "cubicBezier", "$description": "Constant speed" },
      "ease":        { "$value": "ease",                        "$type": "cubicBezier", "$description": "Standard easing" },
      "ease-in":     { "$value": "ease-in",                     "$type": "cubicBezier", "$description": "Accelerate — entering elements" },
      "ease-out":    { "$value": "ease-out",                    "$type": "cubicBezier", "$description": "Decelerate — exiting elements" },
      "ease-in-out": { "$value": "ease-in-out",                 "$type": "cubicBezier", "$description": "Smooth — bidirectional transitions" },
      "spring":      { "$value": "cubic-bezier(0.34,1.56,0.64,1)", "$type": "cubicBezier", "$description": "Physics-based overshoot" },
      "bounce":      { "$value": "cubic-bezier(0.68,-0.55,0.27,1.55)", "$type": "cubicBezier", "$description": "Bounce effect" }
    },
    "delay": {
      "none":       { "$value": "0ms",   "$type": "duration", "$description": "No delay" },
      "short":      { "$value": "100ms", "$type": "duration", "$description": "Small delay — tooltip reveal" },
      "medium":     { "$value": "200ms", "$type": "duration", "$description": "Medium delay — staggered lists" },
      "long":       { "$value": "400ms", "$type": "duration", "$description": "Long delay — orchestrated sequences" },
      "extra-long": { "$value": "800ms", "$type": "duration", "$description": "Extended delay" }
    }
  }
}
